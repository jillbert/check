<% if @error %>
<%= @error.first %>
<% else %>
<h3>Choose your site:</h3>
<ul class="small-block-grid-2">
  <% @sites.each do |s| %>
  <li><%= link_to "#{s['name']}", choose_event_path(:site => "#{s['slug']}"), method: :get %></li>
  <% end %>
</ul>
<% end %>