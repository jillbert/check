<h3>Choose your event:</h3>
<ul class="small-block-grid-2">
  <% @events.each do |e| %>
  <li><%= link_to e["name"], set_event_path(:event => e["id"], :name => e["name"]), method: :get %></li>
  <% end %>
</ul>