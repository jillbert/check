<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- <link rel="stylesheet" href="https://cdn.linearicons.com/free/1.0.0/icon-font.min.css"> -->
    <!-- <link href="//cloud.webtype.com/css/f3a5aac8-1919-4d5c-b564-8fe3712600ae.css" rel="stylesheet" type="text/css" /> -->
    <title><%= content_for?(:title) ? yield(:title) : "Check by cStreet" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <% unless @noheader %>
      <header>
        <%= link_to(image_tag("logo"), landing_path) %>
      </header>
    <% end %>
    <%= yield %>
    <% unless @nofooter %>
      <footer>
        <div class="icons">
          <div class="check-in">
            <%= link_to rsvps_path do %>
              <%= image_tag "Icon/Checkmark/Grey" %>
              <p>Check In</p>
            <% end %>
          </div>
          <div class="add-rsvp">
            <%= link_to new_rsvp_path do %>
              <%= image_tag "Icon/Add/Grey" %>
              <p>Add RSVP</p>
            <% end %>
          </div>
          <div class="settings">
            <%= link_to edit_user_path(current_user) do %>
              <%= image_tag "Icon/Gear/Grey" %>
              <p>Settings</p>
            <% end %>
          </div>
        </div>
      </footer>
    <% end %>
  </body>
</html>
