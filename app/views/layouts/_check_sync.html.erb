<div class="container loading" style="display: none;">
  <div class="logo">
    <% if current_user && current_user.nation.name.include?("columbia") %>
      <%= image_tag(asset_path("logo_caa.svg")) %>
    <% else %>
      <%= image_tag(asset_path("logo.svg")) %>
    <% end %>

  </div>
  <div class="loading-quote">
    <p>“Politics hates a vacuum. If it isn't filled with hope, someone will fill it with fear.”</p>
    <small class="name">Naomi Klein</small>
  </div>
</div>
