<% @page = "index" %>
<div class="container rsvps">
    <div id="rsvp-results">
      <%= render "rsvp_list", letters: @letters, rsvps: @rsvps %>
    </div>
    <div class="show-card" style="display: none;">
      <div class="show-card-close"><%= image_tag(asset_path("X.svg")) %></div>
      <div class="show-card-content"></div>
    </div>
</div>
