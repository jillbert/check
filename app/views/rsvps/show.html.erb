<%= render @rsvp %>

<% if @rsvp.guests.count > 0 %>

Guests:
<% @rsvp.guests.each do |guest| %>
<%= render 'rsvp', rsvp: guest %>
<% end %>

<% end %> 

<% if @guests %>
  <% @guests.each_with_index do |guest, index| %>
  	<div id="new_rsvp_<%= index %>">
    	<h3>New Guest</h3>
    	<%= render "new_rsvp", new_rsvp: guest, host_id: @rsvp.id %>
    </div>
  <% end %>
<% end %>